"""Source root for infra-contract's independently importable modules.

Install the distribution and import public APIs from packages such as
``contracts``, ``engine``, ``providers``, and ``mcp``.
"""
